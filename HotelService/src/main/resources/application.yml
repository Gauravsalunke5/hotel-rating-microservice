server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hotel_rating
    username: postgres
    password: gaurav
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true